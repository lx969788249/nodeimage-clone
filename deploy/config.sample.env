# VPS / Ubuntu 安装脚本配置
#
# 将本文件复制为 config.env 并根据实际情况修改。安装脚本会读取这些变量并自动完成依赖安装、
# 数据库初始化、配置生成、构建、systemd 与 nginx 配置等步骤。

# 站点主域名（不含协议）。如需脚本自动配置 Nginx/HTTPS，请填写域名；
# 如留空，脚本仅启动后端与前端服务，域名及反向代理需自行配置。
DOMAIN=

# PostgreSQL 数据库口令（脚本会创建 nodeimage 用户与数据库）
DB_PASSWORD=change_me_db_pass

# MinIO 凭证（脚本会安装 MinIO 并使用这些凭证）
MINIO_ACCESS_KEY=nodeimage
MINIO_SECRET_KEY=change_me_minio_secret

# API 访问签名与 JWT 密钥（将写入 config/config.yaml）
JWT_ACCESS_SECRET=change_me_access_secret
JWT_REFRESH_SECRET=change_me_refresh_secret
SIGNATURE_SECRET=change_me_signature_secret

# SvelteKit 前端监听端口（在 Nginx 反代后端口不会暴露公网）
FRONTEND_PORT=4173

# API 监听端口（默认 8080）
API_PORT=8080

# Worker 队列名称（通常不需要修改）
REDIS_STREAM=media:ingest

# 是否使用 Let's Encrypt 自动签发证书（yes/no，仅在 DOMAIN 不为空时生效）
ENABLE_HTTPS=yes

# 邮箱地址（Let's Encrypt 使用，需要 ENABLE_HTTPS=yes）
LETSENCRYPT_EMAIL=admin@example.com
